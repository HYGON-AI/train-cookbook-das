Use `dcu-model-train-opt-xuxc` to continue from a confirmed kernel hotspot.

Requirements:

- confirm that the hotspot really belongs on the kernel route
- capture or verify the operator contract
- generate or validate kernel handoff inputs
- use the local RAG knowledge base for kernel-family guidance
- continue into baseline generation and HIP kernel optimization only if the contract is sound

Expected outputs:

- kernel family judgment
- contract summary
- handoff readiness
- kernel optimization next step
