Use `dcu-model-train-opt-xuxc` to optimize this DCU model training workload.

Requirements:

- start from the real project entrypoint
- keep semantic behavior and metric validation stable
- use profiler, advisor, and executor in order
- prefer Python / framework / operator-level improvements before kernel deep optimization
- enter kernel route only when evidence supports it

Expected outputs:

- accepted optimization steps
- rejected or deferred ideas with reasons
- current bottleneck summary
- next recommended optimization direction
